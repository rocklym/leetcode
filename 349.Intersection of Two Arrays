class Solution(object):
    def intersection(self, nums1, nums2):
        ans = []
        for num in nums2:
            if num in nums1:
                ans.append(num)
        return list(set(ans))
